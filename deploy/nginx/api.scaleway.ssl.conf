# =============================================================================
# Nginx — HTTPS (activer UNIQUEMENT après certbot)
# Usage :
#   1. Vérifier HTTP : curl -I http://api.tondomaine.com/api/health
#   2. certbot --nginx -d api.tondomaine.com
#   3. Certbot modifie automatiquement la config — ce fichier est une référence
# =============================================================================

# server {
#     listen 443 ssl http2;
#     server_name api.tondomaine.com;
#
#     ssl_certificate     /etc/letsencrypt/live/api.tondomaine.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.tondomaine.com/privkey.pem;
#
#     root /var/www/admhost/backend/public;
#     index index.php;
#
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     location ~ \.php$ {
#         include snippets/fastcgi-php.conf;
#         fastcgi_pass unix:__PHP_FPM_SOCKET__;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#     }
# }
